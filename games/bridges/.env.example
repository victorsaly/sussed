# Copy to .env for local development.

# Where the shared players service lives.
VITE_PLAYERS_URL=http://localhost:8787

# Set to false to run the game completely offline — no identity calls at all.
VITE_SYNC=false
